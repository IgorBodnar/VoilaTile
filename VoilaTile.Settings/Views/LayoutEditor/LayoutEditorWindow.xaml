<Window x:Class="VoilaTile.Settings.Views.LayoutEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:VoilaTile.Settings.Views"
        WindowStyle="None"
        AllowsTransparency="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Background="{DynamicResource Brush.App.Background.Translucent}"
        KeyDown="Window_KeyDown"
        KeyUp="Window_KeyUp"
        Loaded="Window_Loaded"
        SizeChanged="Window_SizeChanged">

    <Grid>
        <!-- Zone label grid, bound to ViewModel definitions -->
        <Grid x:Name="ZoneLabelGrid"
              IsHitTestVisible="False"
              Background="Transparent"/>

        <!-- Layout editor canvas -->
        <Border BorderBrush="{DynamicResource Brush.Border}" BorderThickness="2" Background="Transparent">
            <Canvas x:Name="RootCanvas" Background="Transparent">

                <!-- Divider lines layer (drawn below) -->
                <Canvas x:Name="DividerLinesLayer"
                        IsHitTestVisible="False" />

                <!-- Divider handles layer (drawn above lines) -->
                <Canvas x:Name="DividerHandlesLayer"
                        Background="Transparent" />
            </Canvas>
        </Border>
    </Grid>
</Window>


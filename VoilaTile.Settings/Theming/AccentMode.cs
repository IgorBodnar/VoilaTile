namespace VoilaTile.Settings.Theming
{
    /// <summary>
    /// Specifies the source of the application's accent color.
    /// </summary>
    public enum AccentMode
    {
        /// <summary>
        /// Uses the Windows system accent color.
        /// </summary>
        Windows,

        /// <summary>
        /// Uses a custom accent color selected by the user.
        /// </summary>
        Custom,
    }
}


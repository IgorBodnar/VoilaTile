<UserControl x:Class="VoilaTile.Configurator.Views.MonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:VoilaTile.Configurator.ViewModels"
             xmlns:views="clr-namespace:VoilaTile.Configurator.Views"
             Width="{Binding MonitorWidth}"
             Height="{Binding MonitorHeight}">
    <Border BorderThickness="10"
            CornerRadius="25"
            Margin="15"
            MouseLeftButtonUp="OnClick">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="BorderBrush" Value="{StaticResource TextWhite}"/>
                <Setter Property="Background" Value="{StaticResource BackgroundLightGray}"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                        <Setter Property="BorderBrush" Value="{StaticResource AccentRed}"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <Grid>
            <!-- Template layout in background -->
            <Viewbox Stretch="Fill">
                <views:ZoneTemplateView DataContext="{Binding SelectedTemplate}" />
            </Viewbox>

            <!-- Monitor label overlay -->
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="{Binding MonitorNumber}"
                           FontSize="180"
                           FontWeight="Bold"
                           HorizontalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{StaticResource TextWhite}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                    <Setter Property="Foreground" Value="{StaticResource AccentRed}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <TextBlock Text="{Binding MonitorSize}"
                           FontSize="120"
                           FontWeight="DemiBold"
                           HorizontalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{StaticResource TextWhite}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                    <Setter Property="Foreground" Value="{StaticResource AccentRed}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>



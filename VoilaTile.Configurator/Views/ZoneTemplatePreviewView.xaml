<UserControl x:Class="VoilaTile.Configurator.Views.ZoneTemplatePreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:VoilaTile.Configurator.ViewModels"
             xmlns:views="clr-namespace:VoilaTile.Configurator.Views"
             xmlns:converters="clr-namespace:VoilaTile.Configurator.Converters"
             MinWidth="150"
             MaxWidth="300"
             HorizontalAlignment="Stretch">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Name}" 
                   FontWeight="Bold" 
                   FontSize="14" 
                   Foreground="White" 
                   Margin="18,10,0,5" 
                   HorizontalAlignment="Left"
                   Visibility="Hidden">
        </TextBlock>

        <Border x:Name="previewBorder"
                Grid.Row="1"
                BorderBrush="Gray"
                BorderThickness="1"
                CornerRadius="5"
                Background="#22FFFFFF"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Top"
                MouseLeftButtonDown="OnClick">

            <Viewbox Stretch="Fill">
                <views:ZoneTemplateView DataContext="{Binding Template}" />
            </Viewbox>
        </Border>
    </Grid>
</UserControl>

